# P01 V1 archive

This preserves the exact 87-image / 168-instance baseline definition without
retraining.  `labels/`, `splits/`, and `metadata/dataset.yaml` were moved from the former
`data/labels`, `data/splits`, and `data/yolo_dataset/dataset.yaml` locations.
Generated V1 YOLO copies remain intentionally untracked and reproducible.
